function checkForName(inputText) {
    // check if user input is text
    if (!isNaN(inputText)) 
  {
    alert("You must input text, the results you're seeing don't make sense");
}
}

export { checkForName }
